# Проверка демо — 23.09.2026

Текущая публичная версия 3: https://ekt-assistant-hackalem-2026.koncevojdanila10.chatgpt.site/

Workflow `ekt_assistant`: 13 узлов, опубликованная версия `65fb5c3c-fd4a-497e-aa14-2c03759b079d`. Ранние проверки ниже сохранены как история версии 2. Секреты и приватные токены корзин в отчёт не включены.

## Пройденные сценарии

| Сценарий | Результат / свидетельство |
| --- | --- |
| Товар `200300273_` | Новый интерфейс получил 26 930 ₸, остаток 36 с распределением по складам и ссылкой на карточку. Ассистент предупредил о расхождении 40 A в описании / 125 A в отдельном поле. Выполнение 3177. |
| Товар без остатка `TEST-000` | Выполнение 3176: явно тестовый товар, stock 0; предложены TEST-002/003/001 с конкретными совпадениями и отличиями. |
| Доставка по-русски | Выполнение 3171: точные тарифы из terms и ссылка на ekt.kz. |
| Доставка по-казахски | Выполнение 3182: те же пороги и тарифы Алматы, ответ на казахском. |
| «Добавь → нет → да» | 3174/3175/3178: отказ удалил pending (cleared_pending=1); последующее «Да, добавь!» не добавило старый товар — ассистент подготовил новое предложение и снова спросил согласие. |
| Отдельное «Да, добавь!» | 3180/3181: TEST-001, 1 шт. добавлена только после отдельного подтверждения; получена ссылка на демо-корзину. |
| Чтение корзины | 3183: get_cart подтвердил ровно 1 шт. TEST-001 и вернул ссылку. Инструмент не изменяет корзину. |
| Запрос 40 шт. при stock 36 | Повторный live-тест: 40 отклонены, ассистент сообщил доступные 36; самовольно товар не добавлялся. |
| Невалидный sessionId с запятыми | 3184: Guard завершил выполнение ошибкой до AI. Нельзя сдвинуть параметры SQL и подменить boolean согласия. |
| Desktop/mobile | Проверены 1280×900 и 390×844. На 390px scrollWidth=clientWidth=390, горизонтального переполнения нет. Новый диалог очищает интерфейс и создаёт отдельную сессию. |
| Формат ответов | Unit: обычный JSON, форматированный JSON, NDJSON с ошибочным Content-Type application/json, кириллица на границах UTF-8 чанков, upstream error/HTML вместо ответа. Реальный POST через браузер прошёл после исправления parser. |
| Безопасное отображение | Нет innerHTML для ответа; javascript: и URL с user:password отклоняются. После сетевой ошибки нет автоматического повторения запроса. |

Измеренные полные ответы в этих smoke-тестах занимали примерно 5.5–14.5 секунд, поток начинает отображаться раньше. Это не нагрузочный тест и не гарантия latency «единицы секунд» для всех запросов.

## Что ещё не выполнено / не проверено

- Это демо-корзина в нашей БД, не настоящая корзина ekt.kz. Нет API изменения корзины и доступа к шаблону сайта партнёра.
- На момент первоначального smoke-теста в базе было восемь реальных товаров. Обновление 23.09.2026: импорт №3191 расширил выборку до всех 40 товаров из предоставленных PDF, TEST-* сохранены. Это всё ещё не весь каталог ekt.kz.
- Полноценный семантический search_products и embeddings реального каталога не реализованы. Есть ILIKE и fallback сходства названий для аналогов; pgvector используется только для совместимых имеющихся векторов.
- Файлы/фото/Excel/Word/PDF как вход чата пока не обрабатываются.
- Наличие сертификата зависит от заполненности БД; выдуманные ссылки не выдаются.
- Docker fresh-install не выполнялся в этой проверке. README уточняет ручные credentials, импорт и публикацию после `docker compose up -d`; статический сайт разворачивается отдельно.
- Нет проведённого нагрузочного теста, независимого аудита всех Supabase grants/RLS и автоматического обновления всего каталога.

Вывод: работающий публичный текстовый MVP для демонстрации основных сценариев; не утверждать, что всё ТЗ выполнено на 100%.


## Дополнение: импорт всей PDF-выборки

Импорт №3191: до 8/40, после 40/40, missing_ids=[]. Все 40 записей после upsert совпали с нормализованными ответами API по артикулу, наименованию, цене, остатку и URL. Проверка №3192 подтвердила чтение нового товара 200300285_ через опубликованного ассистента: 64 920 ₸, 23 шт., склады и предупреждение о расхождении номинального тока.

Дополнительно строгий нормализатор прогнан на всех 40 фактических ответах API. Отрицательные тесты успешно отклоняют пустую цену, пустой/null остаток, boolean вместо цены, отрицательный остаток и null в остатке склада. См. [CATALOG_IMPORT_REPORT.md](CATALOG_IMPORT_REPORT.md).

## Регрессия каталога, поиска и фото — 23.09.2026

- `3197`: «сколько товаров?» → catalog_overview, 40 товаров / 22 позиции в наличии, без get_cart, с оговоркой о выборке.
- `3198`: общий выбор с фото → реальные карточки коробки и Legrand с image_url, ценой, SKU, остатком и product_url.
- `3199`: «автоматический выключатель Legrand» → 17 совпадений, до 5 строк из БД; поиск находит сокращение АВ через нормализованную категорию.
- `3200`: опечатка «каьель силовой» → отсутствие в выборке, не во всём ekt.kz, без подмены кабеля коробкой.
- `3201`: SKU без фото → честное сообщение; при этом тест выявил ошибку SQL в обновлённом find_analogs. Исправлена до публикации и перепроверена ниже.
- `3202`: вопрос о числе товаров на казахском → 40 / 22 на казахском.
- `3203`: RM17UAS16 с нулевым остатком → find_analogs успешно выполнен, доступных аналогов в этой выборке нет; неподходящие типы не предлагаются.
- `3204`: TEST-000 → тестовые аналоги TEST-002/001/003, явная маркировка синтетических данных. Тестовые и реальные товары не смешаны.
- Production `3208`: фото 200300285_ → настоящий URL, 64 920 KZT, 23 шт.; явно обозначен конфликт 160 А в названии и 250 А в атрибуте.
- Последовательный production HTTP-тест в одной новой сессии: «сколько товаров?» → 40/22 (9,25 с), «каьель» → кабеля нет в выборке (9,22 с), «силовой» → силовой кабель, контекст сохранён (11,22 с), «а в моей корзине сколько?» → пустая демо-корзина (6,04 с). Изменяющие инструменты не вызывались.
- Публичный GET каталога: HTTP 200, CORS *, ровно 40 карточек, 5 категорий, 22 в наличии, 39 image_url. SQL read-only; пользовательских данных нет.
- Независимый статический аудит подтвердил: SQL/параметры/связи Guard, prepare_cart_intent, confirm_add_to_cart и чтения корзины сохранены. В экспортах только ссылки на credentials, без секретов и pinData.
- Проверка node config пройдена. Сторонний JSON-линтер ошибочно требует прямое соединение streaming Chat Trigger → Agent и отмечает существующий Guard между ними; защиту не убирали. Реальные HTTP-тесты подтверждают работающий потоковый ответ.

### Опубликованный интерфейс, версия 3

- Публикация завершилась successfully: source `d533ecfd337fcbfe8007dcad5643f8d9ad3666de`, Sites version 3.
- В настоящем браузере: список 40/40; поиск «Legrand автомат» — 18 позиций (включая дифавтомат), с наличием — 17; сброс возвращает 40; только наличие — 22; реле — 14, реле + наличие — 0 с понятным пустым состоянием.
- SKU 310100080_ — одна карточка с текстом «Фото не предоставлено», без чужого изображения. SKU 200300285_ — корректные 64 920 KZT / 23.
- Нажатие «Спросить об этом товаре» отправляет информационный вопрос по SKU. Ответ содержит нужные характеристики и конфликт 160/250 А; изображение в чате действительно загрузилось (`complete=true`, `naturalWidth>0`).
- Визуально проверены настоящие фотографии карточек на desktop и mobile 390×844. На мобильной ширине DOM scrollWidth=clientWidth=375: горизонтального переполнения нет. Временный viewport сброшен после теста.
- `node site/demo/tests.cjs` проходит: запрещённые URL/HTML, `/personal/`, query/hash, обход пути; fallback и повторное использование фото при стриминге; JSON/NDJSON, валидация цен/остатков/числа строк, token-AND поиск и фильтры. Тесты используют искусственные данные только внутри test-файла, не на витрине.
- Правила выбора инструментов уточнены по [OpenAI Docs: function calling](https://developers.openai.com/api/docs/guides/function-calling): назначение, когда вызывать/не вызывать, примеры повторявшихся ошибок. Модель и credentials не менялись.


## Финальная проверка перед сдачей — 23.09.2026, 17:25 (UTC+5)

Рабочие workflow и сайт не изменялись и не перепубликовывались; исправлены только инструкции и отчёт. Проверено состояние GitHub main `a3241fd` с коммитами workflow `9799621` и интерфейса `015dcd7`.

В одной новой изолированной тестовой сессии выполнены **12 production-запросов, все HTTP 200 / n8n success**:

| Выполнение | Проверка | Результат |
| --- | --- | --- |
| 3217 | Сколько товаров? | 40 / 22, оговорка о выборке |
| 3218 | TEST-001 | Явно тестовый товар, 1 850 ₸, 14 шт., характеристики |
| 3219 | TEST-000 без остатка | TEST-002: тот же ток 25 A/1P/C, отличие 10 кА вместо 6 кА |
| 3220 | Доставка, оплата, минимальная партия | Содержательный ответ с источниками условий |
| 3221–3223 | Добавь 1 → нет → показать корзину | Намерение отменено, корзина пуста |
| 3224–3226 | Добавь 1 → отдельное «Да, добавь.» → показать | Ровно 1 TEST-001, итог 1 850 ₸ |
| 3227–3228 | Недопустимое количество 999999 → показать | Запрос отклонён, корзина по-прежнему содержит 1 шт. |

Ссылка из ответа отдельно открыта HTTP-клиентом: HTTP 200, таблица содержит TEST-001 / 1 шт. / 1 850 ₸ и предупреждение о прототипе. Приватный токен и идентификатор сессии в репозиторий не включены. Заказ и оплата на ekt.kz не создавались. Полные ответы заняли 4,84–11,98 с; это smoke-тест, не нагрузочная гарантия.

Повторно: сайт HTTP 200, API каталога HTTP 200 с 40 карточками и CORS; `node site/demo/tests.cjs` PASS на свежих файлах main (Node 24.14.1). Параметры/порядок узлов и connections экспортированного `ekt_assistant` совпадают с опубликованной версией; параметры узлов `ekt_catalog_view` также совпадают. Новых изменений workflow для экспорта нет.

Независимо проверены Compose/env/init-порядок, соответствие cart/terms SQL установочному workflow, безопасные credential-ссылки и отсутствие pinData. В README исправлен пропуск `catalog_overview`, актуализирован поиск, описаны пустая витрина на TEST-only БД, HTTP-сервер сайта и проверочные команды.

**Не пройдено в этой среде:** реальный `docker compose up -d` на пустом томе — Docker CLI/Desktop не установлены. Поэтому статус полного fresh-install остаётся «не проверено», а не PASS. Образ n8n `latest` не фиксирует runtime для будущих запусков; не менять/обновлять рабочий инстанс перед защитой. Ограничения ТЗ из раздела выше сохраняются.
